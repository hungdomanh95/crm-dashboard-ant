import React, { FC, useState } from 'react'
import { Button } from 'antd';
const  SalesPerformance:FC = () => {
  const [date, setDate] = useState(null);

  return (
    <div>
     <Button type="primary">Button</Button>
    </div>
  );
};

export default SalesPerformance